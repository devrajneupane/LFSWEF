/**
 * Total number of balls to start simulation with.
 *
 * @type {number}
 */
export const BALL_COUNT = 100;

/**
 * Initial speed assigned to each ball.
 *
 * @type {number}
 */
export const SPEED = 3;

/**
 * Minimum radius of ball.
 *
 * @type {number}
 */
export const BALL_MIN_RADIUS = 10;

/**
 * Maximum radius of ball.
 *
 * @type {number}
 */
export const BALL_MAX_RADIUS = 20;

/**
 * Highest value for a 24-bit color code in hexadecimal.
 *
 * @type {number}
 */
export const TOTAL_COLOR_COMBINATIONS = 16777215;

/**
 * Maximum number of balls that user can spawn.
 *
 * @type {number}
 */
export const MAX_BALL_COUNT = 1000;

/**
 * Number of balls added to container per click.
 *
 * @type {number}
 */
export const BALLS_PER_CLICK = 5;

/**
 * Frames Per Second
 *
 * @type {number}
 */
export const FPS = 60;
